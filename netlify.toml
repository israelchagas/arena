[build]
  command   = "pnpm run build"
  publish   = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
