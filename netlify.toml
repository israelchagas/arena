[build]
  command   = "pnpm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — todas as rotas apontam para index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
